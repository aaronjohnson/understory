# LaTeX build artifacts
docs/*.aux
docs/*.log
docs/*.out
docs/*.synctex.gz
docs/*.fls
docs/*.fdb_latexmk

# CircuitPython secrets — never commit real credentials
software/settings.toml

# Firmware binaries — downloadable from circuitpython.org
*.bin
*.uf2

# Device runtime data (generated on CIRCUITPY)
log.txt
clock_log.json

# Session artifacts
resume.txt
cp_root_attempt.sh

# OS junk
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.vscode/
docs/*.pdf
